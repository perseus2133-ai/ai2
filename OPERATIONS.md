# ai2 운영 점검과 배포 안내

## 일일 처리 순서

테스트 → 수집 → 원본 데이터 검증 → raw 스냅샷 및 FnGuide 상태 기록 →
컨센서스 보강 → CSV·메타·누적 기록·데일리 선정 저장 → 당일 데이터 재검증 →
모의투자 → 데이터 커밋/푸시 → 카카오 알림.

- 자동 수집은 `main`에서만 실행하며, 동시 실행을 막는다.
- 수집·검증·모의투자 실패 시 Actions는 새 데이터를 푸시하지 않는다.
  앱에는 직전 커밋의 데이터가 남으므로 **수집일을 확인**해야 한다.
- 카카오 전송은 데이터 푸시 후 실행한다. 알림 실패도 run을 실패 처리하지만,
  이미 게시한 정상 데이터는 되돌리지 않는다. 자격증명 미설정은 전송 생략이다.
- 주요 기록은 일부 단일 파일 단위로 임시 파일 후 교체한다. 로컬 파일 전체가
  하나의 트랜잭션은 아니다. 중단된 로컬 수집 결과를 그대로 배포하지 않는다.

## 차단 기준

`data_quality.py`는 다음 경우 예외로 중단한다.

- 두 시장 중 하나 누락, 종목코드 중복·오류, 가격/시가총액 결측·비정상,
  거래량 결측·음수.
- 수집 시 시장별 종목 수가 직전 CSV의 80% 미만.
- 시장별 평균거래량·RSI·5/20일 외국인/기관 수급 또는 업종 매핑 유효 비율이 50% 미만.
- 저장 데이터 검증 시 KST 수집일이 오늘이 아니거나 CSV와 메타의 종목 수 불일치.

FnGuide의 신규 2027/2028 영업이익 추정치가 모두 없거나 헬스 기록에 실패해도
중단한다. 이는 전면 장애 차단이며, 부분적인 컨센서스 누락을 모두 검출하지는 못한다.
50%·80% 기준은 초기 운영 안전선이며 변경 시 정상일/장애일 데이터를 함께 검증한다.

원본 스냅샷은 과거 값 보강 **전**에 저장한다. 컨센서스 보강은 최대 45일이며
가격·수급을 과거 값으로 채우지 않는다. 수집일 확인은 실제 시세의 거래일 검증과
다르다. 공급자가 오래된 시세를 반환하는 경우까지 보장하지 않는다.

## 이번 변경을 배포하기 전에

1. **새 앱 비밀번호 설정:** Streamlit 배포 설정의 Secrets에 `APP_PASSWORD`를
   문자열로 추가하거나 실행 환경변수로 설정한다. 로컬은 `.streamlit/secrets.toml`을
   사용할 수 있다. 값은 코드/커밋/대화에 넣지 않는다. 미설정이면 앱 접근이 차단된다.
   기존 코드에 노출된 비밀번호를 재사용하지 않는다. 과거 Git 기록은 삭제하지 않았다.
2. **카카오 재인증:** 만료 토큰은 로컬에서 `python kakao_auth_setup.py`로 재발급하고
   GitHub Actions의 `KAKAO_REFRESH_TOKEN`을 교체한다. 새 토큰 자동 저장에는
   저장소 Secret 쓰기 권한이 있는 `GH_PAT`가 필요하다. 자동 저장 실패 시 알림을
   실패 처리하며 토큰은 Actions 로그에 출력하지 않는다.
3. **추천 변경 예상:** PER 계산에 시가총액을 전달하고, 정배열 코드 `up`을 일치시켰다.
   다음 정상 수집부터 PER과 추천 점수가 달라질 수 있다. 최근 실적 적자는 더 오래된
   흑자로 대체하지 않는다. 기존 데이터·과거 선정·모의투자 기록은 소급 수정하지 않는다.

## 데일리 점검

```sh
python -m pytest -q
python data_quality.py
python check_fnguide_health.py
git status --short
```

위 명령은 운영 데이터를 갱신하지 않는다. 테스트는 임시 경로와 가짜 HTTP 응답을
사용한다. `python crawl_script.py`와 `python paper_trading.py --auto`는 데이터를
수정하므로 단순 확인용으로 실행하지 않는다.

확인 순서: 마지막 Actions의 실패 단계 → `meta.json` 수집일 → 시장별 건수와 지표
커버리지 → `fnguide_health.json` → 당일 `daily_picks.json` → 모의투자 상태 → 알림.
후보가 없으면 오늘 날짜에 빈 선정 기록을 남기고 과거 추천을 재전송하지 않는다.
손상된 JSON은 임의로 초기화하지 말고 직전 정상 커밋과 비교해 복구한다.
다른 커밋과 충돌하여 자동 푸시가 거절되면 강제 푸시하지 말고 최신 상태에서 재검증한다.

## 아직 남아 있는 투자 검증 과제

이번 수정은 데이터/운영 안정화이며 전략의 수익성을 검증한 것이 아니다.

- 모의매매는 수집 가격(대개 전일 종가)을 사용한다. 실제 다음 체결가, 수수료,
  세금, 슬리피지, 거래정지 등을 반영한 성과와 구분해야 한다.
- 손절·익절 규칙은 현재 주간 리밸런싱 때 평가된다. 일일 감시나 장중 주문이 아니다.
  순위 교체도 하나의 최상위 후보를 여러 보유종목과 비교하므로 실제 대체 매수와
  일대일 대응하지 않을 수 있다. 변경 전 시나리오 검증이 필요하다.
- 데일리 선정과 모의투자는 서로 다른 필터/점수식을 쓴다. 모의투자 수익률을
  데일리 추천의 검증 결과로 보지 않는다.
- PER(실적)은 최근 가용 연간 지배주주 순이익 기준으로 TTM이 아니다.
  Forward PER과 추정 적정가의 가정도 실제 순이익 전망과 별도로 검증해야 한다.
- 컨센서스 표본 수, 추정치 기준일, 수정 방향, 업종·종목 집중도를 후보별로 함께 제시하고,
  시점 고정 데이터로 벤치마크 대비 수익률·낙폭·회전율을 평가하는 작업이 다음 우선순위다.
- 연도가 코드에 고정된 지표가 있으므로 연도 전환 전 별도 점검이 필요하다.

이번 패치에서는 팩터 가중치, 손절·익절 수치, 리밸런싱 주기, 실제 주문 연동을 변경하지 않았다.
